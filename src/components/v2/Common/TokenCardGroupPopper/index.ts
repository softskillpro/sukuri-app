import Popper from '@mui/material/Popper';
import { styled } from '@mui/material/styles';

export const TokenCardGroupPopper = styled(Popper)(() => ({
  marginTop: '13px !important',
  zIndex: 1000,
}));
