import MintHero from '@/components/MintHero';
import { MintContainer } from '@/styles/mint';

const Mint = () => {
  return (
    <MintContainer>
      <MintHero />
    </MintContainer>
  );
};

export default Mint;
